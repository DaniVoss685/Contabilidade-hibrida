node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# QA, scratch e ferramentas temporárias
qa-artifacts/
scratch/
.agents/

# Claude Code local config (per-machine, not shared)
.claude/settings.local.json
.claude/settings.local.json.bak-*

# Isolated agent worktrees (nested git checkouts, never part of the main repo history)
.claude/worktrees/

# Forensic/audit snapshots (schema introspection, may reference real tenant/user identifiers)
.audit/
